<div class="container-fluid d-flex align-items-center justify-content-center vh-100">
  <div class="col-md-6">
    <h1><%= @task.title %></h1>
    <p><%= @task.details %></p>
    <div class="d-flex justify-content-start mb-3">
      <%= link_to 'Back to list', tasks_path, class: 'btn btn-outline-secondary me-2' %>
      <%= link_to 'Edit task', task_edit_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div>
